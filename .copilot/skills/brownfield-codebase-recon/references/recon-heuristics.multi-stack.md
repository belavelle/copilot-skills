# Recon Heuristics (Multi-Stack)

General:
- Prefer CI over README
- Prefer ENTRYPOINT/CMD over assumptions

Node, Java, .NET, Python, Go:
- Look for standard build files
- Entrypoints usually duplicated in scripts and containers

Stop if behavior must be guessed.
