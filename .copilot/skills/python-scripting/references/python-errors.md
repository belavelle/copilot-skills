# Error Handling Patterns

- No bare except.
- Do not ignore exceptions.

Patterns:
- Raise with context (libraries).
- Structured result + exit codes (CLIs).

CLI:
- stderr for errors
- non-zero exit codes
